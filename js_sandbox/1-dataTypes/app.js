// PRIMITIVE TYPES

// String
const name = 'Jane Doe';

// Boolean
const Lali = true;

// Null
const thing = null // Is an primitive type, it will dispaly object but is a primitive type

// undefine
let red;

// Number
const age = 46;

// Symbol
const sym = Symbol()

// console.log(typeof sym);


// REFERENCE TYPES - Objects
// Array
const hobbies = ['movies', 'football', 'games', 'code', 'Reading the Bible'];

// Object literat
const hobbiesObj = {
    movies: 'Game of Thrones',
    football: 'ball',
    games: 'Need for speed',
    code: 'JavaSript',
    Reading: {
        bible: 'Book Of John',
        hygo: 'Njeriu qe qesh'
    }
};

const today = new Date();
console.log(today);

console.log(typeof today);
